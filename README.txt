# HEMS Logger v1.4 (Flight Time Column)
Upload all files to your GitHub repo root. Enable GitHub Pages (main branch, /root).
Open in Safari → Share → Add to Home Screen.

Changes in v1.4:
- Adds Flight Time column (HH:MM) between LAND and END(kg)
- Flight time is auto-calculated (landing - takeoff) or falls back to the entered minutes
- CSV export now includes flightTime column
- Keeps compact, left-aligned log (13px font, 1.2 line-height)